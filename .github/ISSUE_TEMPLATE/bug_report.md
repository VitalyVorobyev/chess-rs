---
name: Bug report
about: Report a problem with chess-corners-rs
labels: bug
---

**Describe the bug**
A clear and concise description of what went wrong.

**To Reproduce**
Steps to reproduce the behavior (minimal code or CLI invocation is ideal):

```bash
cargo run -p chess-corners --release --bin chess-corners -- run ...
```

or a minimal Rust snippet:

```rust
// minimal example here
```

**Expected behavior**
What you expected to happen.

**Environment**

- Rust toolchain (`rustc -V` / `cargo -V`):
- OS:
- Crate version (`chess-corners` / `chess-corners-core`):
- Enabled features (e.g. `image`, `rayon`, `simd`, `tracing`):

**Additional context**
Any extra information, logs, or screenshots that might help.

